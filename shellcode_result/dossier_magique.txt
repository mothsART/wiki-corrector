
In /tmp/shellcheck_file line 17:
	if [ ${crd} = -1 ]; then
             ^----^ SC2086: Double quote to prevent globbing and word splitting.

Did you mean: 
	if [ "${crd}" = -1 ]; then


In /tmp/shellcheck_file line 21:
	if [ ${crd} = 1 ]; then
             ^----^ SC2086: Double quote to prevent globbing and word splitting.

Did you mean: 
	if [ "${crd}" = 1 ]; then


In /tmp/shellcheck_file line 32:
    heure=`date +%D-%H:%m`
          ^--------------^ SC2006: Use $(...) notation instead of legacy backticked `...`.

Did you mean: 
    heure=$(date +%D-%H:%m)


In /tmp/shellcheck_file line 33:
    echo "[${heure}] "${1}" déplacé dans "${2}"" >> ${LOG}
                      ^--^ SC2027: The surrounding quotes actually unquote this. Remove or escape them.
                      ^--^ SC2086: Double quote to prevent globbing and word splitting.
                                          ^--^ SC2027: The surrounding quotes actually unquote this. Remove or escape them.
                                          ^--^ SC2086: Double quote to prevent globbing and word splitting.
                                                    ^----^ SC2086: Double quote to prevent globbing and word splitting.

Did you mean: 
    echo "[${heure}] ""${1}"" déplacé dans ""${2}""" >> "${LOG}"


In /tmp/shellcheck_file line 57:
    cd "${1}"
    ^-------^ SC2164: Use 'cd ... || exit' or 'cd ... || return' in case cd fails.

Did you mean: 
    cd "${1}" || exit


In /tmp/shellcheck_file line 64:
			ls -d */ 2> /dev/null > /tmp/tri
                              ^-- SC2035: Use ./*glob* or -- *glob* so names with dashes won't become options.


In /tmp/shellcheck_file line 66:
			while read dossier
                              ^--^ SC2162: read without -r will mangle backslashes.


In /tmp/shellcheck_file line 76:
    while read fichier
          ^--^ SC2162: read without -r will mangle backslashes.


In /tmp/shellcheck_file line 84:
				type=`file -bi "${fichier}"`
                                     ^---------------------^ SC2006: Use $(...) notation instead of legacy backticked `...`.

Did you mean: 
				type=$(file -bi "${fichier}")


In /tmp/shellcheck_file line 87:
		            *script*) bouge "${fichier}" "${BIN}";;
                            ^------^ SC2221: This pattern always overrides a later one on line 91.


In /tmp/shellcheck_file line 91:
		            *pdf* | *dvi* | *postscript*) bouge "${fichier}" "${PDF}";;
                                            ^----------^ SC2222: This pattern never matches because of a previous pattern on line 87.


In /tmp/shellcheck_file line 104:
				        type=`file -b "${fichier}"`
                                             ^--------------------^ SC2006: Use $(...) notation instead of legacy backticked `...`.

Did you mean: 
				        type=$(file -b "${fichier}")


In /tmp/shellcheck_file line 136:
    [ $OPT = "-" ] && case "${OPTARG%%=*}" in
      ^--^ SC2086: Double quote to prevent globbing and word splitting.

Did you mean: 
    [ "$OPT" = "-" ] && case "${OPTARG%%=*}" in


In /tmp/shellcheck_file line 184:
OLDDIR=`pwd`
       ^---^ SC2006: Use $(...) notation instead of legacy backticked `...`.

Did you mean: 
OLDDIR=$(pwd)


In /tmp/shellcheck_file line 207:
	echo "  les sous-dossiers où classer les fichiers triés. C'est le dossier CIBLE." >> /tmp/notice
        ^-- SC2129: Consider using { cmd1; cmd2; } >> file instead of individual redirects.


In /tmp/shellcheck_file line 254:
	TABSCE=($(zenity --file-selection --title "Dossier Magique - Choisir répertoire(s) SOURCE" --filename "$DIR"/ --directory --multiple))
                ^-- SC2207: Prefer mapfile or read -a to split command output (or quote to avoid splitting).


In /tmp/shellcheck_file line 285:
cd "${OLDDIR}"
^------------^ SC2164: Use 'cd ... || exit' or 'cd ... || return' in case cd fails.

Did you mean: 
cd "${OLDDIR}" || exit

For more information:
  https://www.shellcheck.net/wiki/SC2027 -- The surrounding quotes actually u...
  https://www.shellcheck.net/wiki/SC2164 -- Use 'cd ... || exit' or 'cd ... |...
  https://www.shellcheck.net/wiki/SC2207 -- Prefer mapfile or read -a to spli...


