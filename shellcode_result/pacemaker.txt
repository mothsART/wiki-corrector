
In /tmp/shellcheck_file line 4:
CRITICAL_STATE=2
^------------^ SC2034: CRITICAL_STATE appears unused. Verify use (or export if used externally).


In /tmp/shellcheck_file line 7:
cnt=`sudo crm_mon -1f | grep -q fail-count`
^-^ SC2034: cnt appears unused. Verify use (or export if used externally).
    ^-- SC2006: Use $(...) notation instead of legacy backticked `...`.

Did you mean: 
cnt=$(sudo crm_mon -1f | grep -q fail-count)


In /tmp/shellcheck_file line 9:
if [ $? -eq 0 ]
     ^-- SC2181: Check exit code directly with e.g. 'if mycmd;', not indirectly with $?.

For more information:
  https://www.shellcheck.net/wiki/SC2034 -- CRITICAL_STATE appears unused. Ve...
  https://www.shellcheck.net/wiki/SC2006 -- Use $(...) notation instead of le...
  https://www.shellcheck.net/wiki/SC2181 -- Check exit code directly with e.g...


